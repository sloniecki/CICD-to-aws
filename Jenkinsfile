pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                git(url: 'https://github.com/sloniecki/project.git', branch: 'main')
            }
        }
    }
}
